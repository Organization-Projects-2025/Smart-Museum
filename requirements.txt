# Prefer python/requirements.txt (bleak + Windows notes). This file mirrors core deps.
# Windows face_recognition: see comment block in python/requirements.txt (dlib-bin + --no-deps).

numpy>=1.24,<3
opencv-python>=4.8,<5
bleak>=0.21.0
face_recognition>=1.3.0
dlib-bin>=20.0.0; platform_system == "Windows"
mediapipe>=0.10.0

