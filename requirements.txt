# ============================================================================
# Smart Museum — Python Dependencies
# ============================================================================
#
# Install (from repo root):
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   pip install -U pip
#   pip install -r requirements.txt
#   pip install "face_recognition>=1.3.0" --no-deps
#
# Then run:
#   start.bat
# ============================================================================

# ── Core ──────────────────────────────────────────────────────────────────────
numpy>=1.24,<3
opencv-python>=4.8,<5
Pillow>=9.0.0

# ── Face recognition (port 5000 — auth) ──────────────────────────────────────
face-recognition-models>=0.3.0
dlib-bin>=20.0.0; platform_system == "Windows"
# face_recognition itself must be installed with --no-deps (see header above)

# ── Bluetooth (port 5000 — optional) ─────────────────────────────────────────
pybluez2>=0.40

# ── MediaPipe — gaze, hand tracking, gesture ─────────────────────────────────
mediapipe>=0.10.30

# ── HSEmotion — facial expression recognition (port 5002) ────────────────────
# Uses EfficientNet-B0 trained on AffectNet via ONNX.
hsemotion-onnx>=0.3.0

# ── DeepFace — demographic analysis (age, gender, race) during sign-up ────────
deepface>=0.0.89

# ── Gesture recognition (port 5001) ──────────────────────────────────────────
dollarpy>=0.1.0

# ── YOLO object detection (port 5003) ────────────────────────────────────────
# Set YOLO_CONTEXT_MOCK=1 in start.bat to skip YOLO and use fake tracks instead.
ultralytics>=8.0.0

# ── Notes ─────────────────────────────────────────────────────────────────────
# DeepFace is used for demographic analysis (age, gender, race) at sign-up.
# HSEmotion handles real-time emotion detection (separate concern).
# test_unified_server.py and unified_museum_server.py have been removed;
#   the entry point is now:  python python/server/main.py  (or start.bat)
#
# Service → port mapping:
#   auth_service.py          5000   face ID + bluetooth
#   gesture_service.py       5001   dollarpy gesture recognition
#   gaze_emotion_service.py  5002   mediapipe gaze + hsemotion emotions
#   yolo_service.py          5003   yolov8 object context
#   hand_service.py          5004   mediapipe hand pose
