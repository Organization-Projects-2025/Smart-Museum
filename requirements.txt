# Root Python environment for python_server.py
# Install with: .\.venv\Scripts\python.exe -m pip install -r requirements.txt

# Core runtime used directly in python_server.py
numpy>=1.24,<3
opencv-python>=4.8,<5

# Optional in code (server still runs if missing, but related features return ERROR)
pybluez2>=0.46
face_recognition>=1.3.0

# Helps Windows installs for face_recognition (binary wheels)
dlib-bin>=20.0.0; platform_system == "Windows"

# Used by the separate hand-tracking pipeline (mediapipe/hand_tracker.py)
mediapipe>=0.10.0
