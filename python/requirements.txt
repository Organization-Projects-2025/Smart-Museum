# Full dependency list is in the repo root: ../requirements.txt
#
# Easiest: from repo root run (creates .venv if needed and installs everything):
#   powershell -ExecutionPolicy Bypass -File .\install_python_deps.ps1
#
# Or:  .\.venv\Scripts\python.exe -m pip install -r ..\requirements.txt
#       .\.venv\Scripts\python.exe -m pip install "face_recognition>=1.3.0" --no-deps

-r ../requirements.txt
