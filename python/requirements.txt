# Root Python environment for python/server/python_server.py
# Install with: .\.venv\Scripts\python.exe -m pip install -r python\requirements.txt
#
# Windows: plain pip pulls source dlib and needs CMake. Use instead:
#   .\.venv\Scripts\python.exe -m pip install "numpy>=1.24,<3" "opencv-python>=4.8,<5" bleak "dlib-bin>=20.0.0" "mediapipe>=0.10.0"
#   .\.venv\Scripts\python.exe -m pip install "face_recognition>=1.3.0" --no-deps
#   .\.venv\Scripts\python.exe -m pip install "face-recognition-models>=0.3.0" "Click>=6.0"

# Core runtime used directly in python/server/python_server.py
numpy>=1.24,<3
opencv-python>=4.8,<5

# Optional in code (server still runs if missing, but related features return ERROR)
# pybluez2>=0.46  # Removed - doesn't work on Windows
bleak>=0.21.0  # Modern cross-platform Bluetooth library for Windows
face_recognition>=1.3.0

# Helps Windows installs for face_recognition (binary wheels) - MUST come before face_recognition
dlib-bin>=20.0.0; platform_system == "Windows"

# Used by the separate hand-tracking pipeline (python/hand_tracking/hand_tracker.py)
mediapipe>=0.10.0
