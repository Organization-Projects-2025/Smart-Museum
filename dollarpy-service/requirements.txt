# Smart Museum — Dollarpy gesture service
#
# Recommended (full museum Python stack, same versions as python_server / gaze):
#   cd ..
#   python -m pip install -r requirements.txt
#   python -m pip install "face_recognition>=1.3.0" --no-deps
#
# Minimal install (this folder only — gesture GUI + dollarpy):
opencv-python>=4.8,<5
mediapipe>=0.10.30,<0.11
numpy>=1.24,<3
dollarpy>=0.1.0
Pillow>=9.0.0
